# Petstore API Tests (Yandex Fintech Internship)

Пример проекта автотестов для публичного API **Swagger Petstore**.  
Задача выполнена в рамках тестового задания на позицию стажёра  
«Инженер по автоматизации тестирования в Финтех».

В проекте используются:
- **Python 3**
- **pytest** — фреймворк для тестирования
- **requests** — библиотека для HTTP-запросов

## 🚀 Возможности проекта

Проект содержит примеры функциональных тестов для API:

- **POST /pet** — создание питомца  
- **GET /pet/{petId}** — получение питомца по ID  
- **PUT /pet** — обновление данных питомца  
- **DELETE /pet/{petId}** — удаление питомца  
- **GET /pet/findByStatus** — поиск по статусу  
- Негативный тест на получение несуществующего питомца  

Структура проекта позволяет расширить тесты до сотен сценариев
и добавить поддержку других секций API (`/store`, `/user`).

## 📁 Структура проекта

```text
.
├── api/
│   ├── __init__.py
│   └── pet_client.py        # простой клиент для работы с API /pet
├── tests/
│   ├── __init__.py
│   └── test_pet_crud.py     # набор тестов для CRUD и поиска
├── conftest.py              # фикстуры: base_url + генерация данных
├── requirements.txt
└── README.md
